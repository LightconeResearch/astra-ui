{
  "name": "astra-ui-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Composable React components for resolved ASTRA analyses.",
  "license": "BSD-3-Clause",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "npm run clean --workspace @lightcone-research/astra-ui",
    "build": "npm run build --workspace @lightcone-research/astra-ui",
    "typecheck": "npm run typecheck --workspace @lightcone-research/astra-ui",
    "test": "npm run build && node --test tests/*.test.mjs",
    "test:packages": "node --test tests/package-contract.test.mjs"
  },
  "devDependencies": {
    "@astra-spec/sdk": "file:../astra-typescript",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.7.0"
  }
}
